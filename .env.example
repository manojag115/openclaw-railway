# ---------------------------------------------------------------------------
# .env.example — copy this to .env for local docker-compose testing,
# or set these directly as Railway environment variables.
# ---------------------------------------------------------------------------

# ---------------------------------------------------------------------------
# Provider — use ONE of the two blocks below.
#
# Option A: Anthropic (requires paid credits at console.anthropic.com)
# ---------------------------------------------------------------------------
ANTHROPIC_API_KEY=sk-ant-...

# Option B: OpenAI (has a free $5 tier for new accounts at platform.openai.com)
# If you use this, also set MODEL below to an OpenAI model.
# OPENAI_API_KEY=sk-...

# ---------------------------------------------------------------------------
# Model — which model to use.
#   Anthropic examples : anthropic/claude-opus-4-5
#                         anthropic/claude-sonnet-4-5
#   OpenAI examples    : openai/gpt-4o
#                         openai/gpt-4o-mini        ← cheapest, good for testing
#
# Default (if blank)  : anthropic/claude-opus-4-5
# ---------------------------------------------------------------------------
MODEL=

# ---------------------------------------------------------------------------
# Auth
# ---------------------------------------------------------------------------
# If blank, a random token is generated on first boot and printed to deploy
# logs. Set this if you want a stable, known token.
GATEWAY_TOKEN=

# OPTIONAL — channels (add any you want)
TELEGRAM_BOT_TOKEN=
DISCORD_BOT_TOKEN=
SLACK_BOT_TOKEN=
SLACK_APP_TOKEN=
